<!--pages/subject/subject.wxml-->
<view class='movie-type'>
    <view wx:for="{{type}}" wx:key="{{index}}" data-title="{{item.title}}" bindtap='totypeHander'>
     <image src='/assets/image/{{item.url}}' mode="widthFix" class='pic'></image>
     <text>{{item.title}}</text>
    </view>
</view>
